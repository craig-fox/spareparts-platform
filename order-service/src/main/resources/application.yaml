spring:
  application:
    name: order-service

  datasource:
      url: jdbc:postgresql://localhost:5434/orderdb
      username: order
      password: order

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

server:
    port: 8082

customer-service:
    base-url: http://localhost:8081
